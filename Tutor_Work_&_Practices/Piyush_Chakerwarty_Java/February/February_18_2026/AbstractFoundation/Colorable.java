// Create an interface named Colorable.

public interface Colorable {
    // The Purpose: This interface specifies a behavior for classes that can be filled with color. 
    // Method: Define a method header void howToColor(). Remember, this method is public by default and has no body.

    public void howToColor();

}
